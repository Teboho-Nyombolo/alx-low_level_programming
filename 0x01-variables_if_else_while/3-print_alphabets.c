#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	char c;

	char f;

	c = 'a';
	f = 'A';
	while
		(c <= 'z') {
			putchar(c);
			c++
		}
	while
		(f <= 'Z') {
			putchar(f);
			f++
		}
	putchar('\n');
	return (0);
}
